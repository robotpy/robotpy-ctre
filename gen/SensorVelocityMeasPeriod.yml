---

enums:
  SensorVelocityMeasPeriod:
    subpackage: sensors
classes:
  SensorVelocityMeasPeriodRoutines:
    subpackage: sensors
    ignore: true  # enum toString only
    shared_ptr: false
    methods:
      toString:
