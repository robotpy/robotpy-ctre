import enum

{% for header in headers %}
{% for enum in header.enums %}
class {{ enum.name }}(enum.IntEnum):
{% for val in enum['values'] %}
    {{ val.x_name }} = {{ val.value | replace(' ', '') }}
{% endfor %}
{% endfor %}

{% endfor %}
